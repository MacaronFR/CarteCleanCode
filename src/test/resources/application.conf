ktor{
    development = true
    modules = [ fr.imacaron.cleancode.MainKt.cleanCode ]
    deployment {
        port = 8080
    }
}

database{
    url = ${BDD_URL}
    user = ${BDD_USER}
    password = ${BDD_PASSWORD}
}